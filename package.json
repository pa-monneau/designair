{
  "name": "designair",
  "version": "0.2.15",
  "private": true,
  "description": "Lib UI React partagée entre les apps (Record'air, Home'air, Bi'air), publiée sous le scope npm historique @recordair.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "storybook": "npm run build && storybook dev -p 6006 --no-open",
    "build-storybook": "npm run build && storybook build",
    "test": "npm run build && npm run test:clear-cache && vitest run --project=storybook",
    "test:watch": "npm run build && npm run test:clear-cache && vitest --project=storybook",
    "test:coverage": "npm run build && npm run test:clear-cache && vitest run --project=storybook --coverage",
    "test:clear-cache": "rm -rf node_modules/.cache/storybook/*/*/sb-vitest",
    "pack:dry-run": "npm pack --cache .npm-cache --workspaces --dry-run",
    "release:publish": "bash scripts/publish-packages.sh"
  },
  "devDependencies": {
    "@storybook/addon-a11y": "^10.5.4",
    "@storybook/addon-docs": "^10.5.4",
    "@storybook/addon-vitest": "^10.5.10",
    "@storybook/react-vite": "^10.5.4",
    "@tailwindcss/postcss": "^4.1.0",
    "@types/node": "^24.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitest/browser-playwright": "^4.1.11",
    "@vitest/coverage-v8": "^4.1.11",
    "playwright": "^1.62.1",
    "postcss": "^8.5.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "storybook": "^10.5.4",
    "tailwindcss": "^4.1.0",
    "typescript": "^5.9.0",
    "vite": "^7.0.0",
    "vitest": "^4.1.11"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
